# apps/web/app/c/[clientId]/settings/page.tsx

- SettingsPage · function · L17-L51 — async function SettingsPage({ params, searchParams, }: { params: Promise<{ clientId: string }>; searchParams: Promise<{ tab?: string }>; })
