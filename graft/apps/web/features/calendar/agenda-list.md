# apps/web/features/calendar/agenda-list.tsx

- AgendaList · function · L11-L63 — function AgendaList({ days, byDay, month, brand, onOpen, }: { days: Date[]; byDay: Map<string, Post[]>; month: Date; brand: BrandKit; onOpen: (postId: string) => void; })
