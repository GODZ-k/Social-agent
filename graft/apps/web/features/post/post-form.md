# apps/web/features/post/post-form.tsx

- PostForm · function · L11-L74 — function PostForm({ form, locked, mediaUrl, onSubmit, children, }: { form: UseFormReturn<Values>; /** A published post is read-only. */ locked: boolean; mediaUrl: string | null; onSubmit: (values: Values) => void; children?: React.ReactNode; })
