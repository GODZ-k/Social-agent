# apps/web/features/settings/delete-client.tsx

- DeleteClient · function · L13-L74 — function DeleteClient({ client, isAdmin, }: { /** Only what the confirmation reads: the name it shows and the posts it cancels. */ client: Pick<Client, "id" | "name" | "stats">; isAdmin: boolean; })
