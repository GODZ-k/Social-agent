# apps/web/components/shell/top-bar.tsx

- switchable · function · L12-L14 — function switchable({ id, name, accent }: Client): SwitchableClient
- TopBar · function · L16-L45 — function TopBar({ viewer, client, clients = [] }: { viewer: Viewer; client?: Client; clients?: Client[] })
