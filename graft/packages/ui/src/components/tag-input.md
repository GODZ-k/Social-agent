# packages/ui/src/components/tag-input.tsx

- TagInputProps · interface · L7-L18 — interface TagInputProps
- TagInput · function · L25-L85 — function TagInput({ value, onChange, normalize = (raw) => raw.trim() || null, max = Infinity, placeholder, disabled, ...field }: TagInputProps)
- commit · function · L29-L37 — function commit(raw: string)
